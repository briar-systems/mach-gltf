[project]
id = "gltf"
version = "0.3.0"
src = "src"
out = "out/{target.name}/{profile.name}"

[target.linux-x86_64]
default = true
isa = "x86_64"
os  = "linux"
abi = "sysv64"

[target.windows]
isa = "x86_64"
os  = "windows"
abi = "win64"

[target.darwin]
isa = "x86_64"
os  = "darwin"
abi = "sysv64"

[profile.debug]
default = true
opt = 0
debug = true
simd = "scalarize"
vectorize = false
float_reassoc = false

[profile.release]
opt = 2
debug = false
simd = "scalarize"
vectorize = false
float_reassoc = false

[artifact.gltf]
kind = "static"
entry = "gltf.mach"
out = "lib/gltf"
targets = ["*"]
link = []
need = []
default = true

[dep.std]
git = "https://github.com/briar-systems/mach-std"
ref = "tag/v2.1.0"
